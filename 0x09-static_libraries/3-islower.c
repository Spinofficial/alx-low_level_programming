#include "main.h"

/**
 * _islower - This check for lower cases characters
 * @c: lowercase character to check for
 * Return: 1 if c is lower case character, or 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
